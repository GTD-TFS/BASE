# BASE

Visor moderno de `denupolInbox`:
- Login Firebase
- Lista de tokens/documentos
- Selección de elemento
- Descarga JSON del seleccionado

## Ejecutar local

Sirve la carpeta por HTTP, por ejemplo:

```bash
cd BASE
python3 -m http.server 8780
```

Luego abre `http://127.0.0.1:8780`.
